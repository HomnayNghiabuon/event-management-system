# Copy thành .env nếu muốn override, hoặc đặt trực tiếp trên Vercel (Settings → Environment Variables)
# Base URL của backend API. Để trống khi dev local (mặc định http://localhost:8081/api/v1)
VITE_API_URL=
